/* Generated by im-code-map v5.1. Live child-file composition. */
const OUTPUT_FOLDER = "architecture/atlas/excalidraw";
const placements = [{"diagram_id": "domain-commerce-overview", "x": 0, "y": 0, "scale": 1, "title": "Responsibility map", "path": "architecture/atlas/excalidraw/domain-commerce-overview.excalidraw"}, {"diagram_id": "stream-member-place-order", "x": 0, "y": 1200, "scale": 1, "title": "Primary stream", "path": "architecture/atlas/excalidraw/stream-member-place-order.excalidraw"}, {"diagram_id": "state-product-order-eligibility", "x": 0, "y": 3000, "scale": 1, "title": "State and eligibility", "path": "architecture/atlas/excalidraw/state-product-order-eligibility.excalidraw"}];
ea.reset();
ea.setTheme(0);
ea.setFontFamily(2);
ea.canvas.viewBackgroundColor = "#F7F8FC";
ea.style.strokeColor = "#101828";
ea.style.fontSize = 32;
ea.addText(40,30,"Commerce map (Obsidian live embeds)",{width:1800,height:70,textAlign:"left",textVerticalAlign:"middle"});
for (const p of placements) {
  const file = app.vault.getAbstractFileByPath(p.path);
  if (!file) throw new Error(`Missing child Excalidraw file: ${p.path}`);
  const width = Math.max(900, 1800*p.scale);
  const height = Math.max(600, 1100*p.scale);
  ea.style.strokeColor = "#343a40";
  ea.style.fontSize = 22;
  ea.addText(p.x+40,p.y+105,p.title,{width:width,height:42,textAlign:"left",textVerticalAlign:"middle"});
  ea.addEmbeddable(p.x+40,p.y+160,width,height,undefined,file);
}
await ea.create({filename:"master-commerce-embeddable",foldername:OUTPUT_FOLDER,onNewPane:true});
